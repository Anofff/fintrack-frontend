# Backend API base URL. Must include /api/v1 (app will append it if missing).
# Local:
VITE_API_URL=http://localhost:3000/api/v1
# Production (Vercel / Netlify — set in host env, then rebuild):
# VITE_API_URL=https://your-backend.railway.app/api/v1
